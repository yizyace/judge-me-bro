.DS_Store

# node / typescript
node_modules/
dist/
*.tsbuildinfo

# local data (reputation ledger, run artifacts)
data/
runs/

# nested git worktrees (parallel agent workspaces)
.claude/worktrees/

# local env / secrets (Valiron operator API key, etc. — see AGENTS.md)
.env
.env.*
!.env.example

